/**
 * Control Server — REMOVIDO
 * O painel de controle remoto foi removido por motivos de segurança.
 * Esta função é mantida como no-op para não quebrar imports existentes.
 */
export async function startControlServer(): Promise<void> {
  // No-op: painel de controle remoto removido
}
